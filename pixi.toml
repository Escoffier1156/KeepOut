[workspace]
name = "keepout"
version = "1.0.0"
description = "Lightweight code protection CLI powered by Smart Syntax Locking and Property-Based Testing."
authors = ["Escoffier1156"]
channels = ["conda-forge"]
platforms = ["linux-64"]

[dependencies]
python = "3.11.*"
click = ">=8.0.0"
pytest = ">=8.0.0"
pip = "*"
